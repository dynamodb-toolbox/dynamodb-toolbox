export const $item = Symbol('$item')
/** Symbol key for the item to put. */
export type $item = typeof $item

export const $options = Symbol('$options')
/** Symbol key for a `PutItemCommand`'s options. */
export type $options = typeof $options
