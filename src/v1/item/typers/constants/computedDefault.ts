/**
 * Tag for attributes with computed default values
 */
export const ComputedDefault = Symbol('Tag for attributes with computed default values')

/**
 * Tag for attributes with computed default values
 */
export type ComputedDefault = typeof ComputedDefault
