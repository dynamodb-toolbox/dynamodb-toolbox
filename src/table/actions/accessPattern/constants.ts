export const $schema = Symbol('$schema')
/** Symbol key for an access pattern's input schema. */
export type $schema = typeof $schema

export const $pattern = Symbol('$pattern')
/** Symbol key for an access pattern's transform function. */
export type $pattern = typeof $pattern

export const $options = Symbol('$options')
/** Symbol key for an access pattern's default query options. */
export type $options = typeof $options

export const $meta = Symbol('$meta')
/** Symbol key for an access pattern's metadata. */
export type $meta = typeof $meta
