/**
 * Tag for properties with computed default values
 */
export const ComputedDefault = Symbol('Tag for properties with computed default values')

/**
 * Tag for properties with computed default values
 */
export type ComputedDefault = typeof ComputedDefault
